/**
 * @fileoverview Shared pill-button class strings, so the primary/secondary
 * button look stays identical everywhere it's used (homepage hero,
 * app-preview CTAs, etc) rather than drifting across copies.
 */
export const PRIMARY_BTN_CLASS =
  'bg-brew-accent text-brew-bg font-bold text-sm px-7 py-3 rounded-lg hover:bg-[#D4F27A] transition-colors tracking-wide';

export const SECONDARY_BTN_CLASS =
  'border border-brew-accent text-brew-accent font-bold text-sm px-7 py-3 rounded-lg hover:bg-brew-accent/10 transition-colors tracking-wide';
